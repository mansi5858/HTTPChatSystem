# Optional: use a single connection string instead of individual vars
# DATABASE_URL=postgresql://chat:chatsecret@localhost:5432/httpchat

# Or set PostgreSQL connection explicitly
# PGHOST=localhost
# PGPORT=5432
# PGUSER=chat
# PGPASSWORD=chatsecret
# PGDATABASE=httpchat

# Server port (default 3000)
# PORT=3000
